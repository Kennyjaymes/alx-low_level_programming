#include "main.h"

/**
 * main - check the code
 * Description: function that multiplies two integers 
 * @a: int type number
 * @b: int type number
 * Return: 0.
 */
int mul(int a, int b)
{
		return (a * b);
}
